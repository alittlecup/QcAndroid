<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="105dp"
    android:layout_height="90dp"
    android:background="@drawable/bg_rect_corner_primary"
    >

  <RelativeLayout
      android:layout_width="match_parent"
      android:layout_height="match_parent"
      >

  <LinearLayout
      android:id="@+id/charge_layout"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:gravity="center"
      android:minHeight="105dp"
      android:orientation="vertical"
      android:paddingBottom="@dimen/activity_vertical_margin"
      android:paddingTop="@dimen/activity_vertical_margin"

      >

    <TextView
        android:id="@+id/title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/colorPrimary"
        tools:text="1200元"
        android:lines="1"
        />

    <TextView
        android:id="@+id/real_income"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:ellipsize="marquee"
        android:textColor="@color/colorPrimary"
        tools:text="(售价1200元)"
        android:singleLine="true"
        />

    <TextView
        android:id="@+id/valid_date"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:textColor="@color/colorPrimary"
        android:textSize="11sp"
        android:lines="1"
        tools:text="有效期:300天"
        />

    <TextView
        android:id="@+id/support_type"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:textColor="@color/colorPrimary"
        android:textSize="11sp"
        android:lines="1"
        tools:text="适用于"
        />

  </LinearLayout>

  <ImageView
      android:id="@+id/chosen"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_alignRight="@id/charge_layout"
      android:layout_alignTop="@id/charge_layout"
      app:srcCompat="@drawable/vd_chosen_hook"
      tools:src="@drawable/vd_chosen_hook"
      />

  <ImageView
      android:id="@+id/tag_only_staff"
      android:layout_width="24dp"
      android:layout_height="24dp"
      android:layout_margin="8dp"
      android:visibility="gone"
      app:srcCompat="@drawable/vd_module_staff_grey"

      />

    <View
        android:id="@+id/shadow_card_option"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/colorPrimary"
        android:alpha="0.1"
        android:visibility="gone"/>

  </RelativeLayout>
</FrameLayout>
